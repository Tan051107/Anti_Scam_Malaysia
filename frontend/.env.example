# Public URL of the deployed backend API. Leave unset during local development
# to use Vite's /api proxy.
VITE_API_BASE_URL=https://your-api-domain.example/api

# Backend URL used only by `npm run dev` for the Vite /api proxy.
VITE_API_PROXY_TARGET=http://localhost:8000
